# Vaultwarden Backup Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tägliches automatisches Backup des Vaultwarden Docker Volumes nach `/home/ubhp-nova/backups/docker/` mit 14-Tage-Rotation.

**Architecture:** Ein Shell-Skript (`backup.sh`) exportiert das Named Volume `${AGENT_NAMESPACE}-pjc3docker-vol-vw-data` via temporärem Alpine-Container als `.tar.gz` in einen datierten Unterordner. Ein Cron-Eintrag ruft das Skript täglich um 02:00 Uhr auf. Ein Makefile-Target ermöglicht manuellen Aufruf.

**Tech Stack:** Bash, Docker (alpine image), cron

---

## Chunk 1: backup.sh + Backup-Ordner

### Task 1: Backup-Ordner anlegen

**Files:**
- Create: `/home/ubhp-nova/backups/docker/` (Verzeichnis, nicht committed)

- [ ] **Step 1: Verzeichnis anlegen**

```bash
mkdir -p /home/ubhp-nova/backups/docker/
```

- [ ] **Step 2: Prüfen ob Ordner existiert**

```bash
ls -la /home/ubhp-nova/backups/
```

Erwartet: `docker/` Verzeichnis vorhanden

---

### Task 2: backup.sh schreiben

**Files:**
- Create: `/home/ubhp-nova/claude-c/pjc3-docker/backup.sh`

- [ ] **Step 1: Skript anlegen**

```bash
cat > /home/ubhp-nova/claude-c/pjc3-docker/backup.sh << 'EOF'
#!/usr/bin/env bash
set -euo pipefail

# Backup-Konfiguration
AGENT_NAMESPACE="${AGENT_NAMESPACE:-CLAUDE}"
VOLUME_NAME="${AGENT_NAMESPACE}-pjc3docker-vol-vw-data"
BACKUP_ROOT="/home/ubhp-nova/backups/docker"
DATE="$(date +%Y-%m-%d)"
BACKUP_DIR="${BACKUP_ROOT}/${DATE}"
RETAIN_DAYS=14

echo "[$(date '+%Y-%m-%d %H:%M:%S')] Backup gestartet: ${VOLUME_NAME}"

# Zielordner anlegen
mkdir -p "${BACKUP_DIR}"

# Volume prüfen
if ! docker volume inspect "${VOLUME_NAME}" > /dev/null 2>&1; then
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] FEHLER: Volume '${VOLUME_NAME}' nicht gefunden." >&2
  exit 1
fi

# Volume exportieren
docker run --rm \
  -v "${VOLUME_NAME}:/data:ro" \
  -v "${BACKUP_DIR}:/backup" \
  alpine \
  tar czf "/backup/vw-data.tar.gz" -C /data .

echo "[$(date '+%Y-%m-%d %H:%M:%S')] Backup gespeichert: ${BACKUP_DIR}/vw-data.tar.gz"

# Integrität prüfen
if ! tar tzf "${BACKUP_DIR}/vw-data.tar.gz" > /dev/null 2>&1; then
  echo "[$(date '+%Y-%m-%d %H:%M:%S')] FEHLER: Backup-Archiv defekt." >&2
  exit 1
fi

echo "[$(date '+%Y-%m-%d %H:%M:%S')] Archiv-Integritaet OK."

# Alte Backups loeschen (>14 Tage)
find "${BACKUP_ROOT}" -maxdepth 1 -type d -mtime +${RETAIN_DAYS} -exec rm -rf {} +

echo "[$(date '+%Y-%m-%d %H:%M:%S')] Backup abgeschlossen."
EOF
```

- [ ] **Step 2: Skript ausführbar machen**

```bash
chmod +x /home/ubhp-nova/claude-c/pjc3-docker/backup.sh
```

- [ ] **Step 3: Skript manuell testen (Dry-Run ohne laufenden Stack)**

```bash
cd /home/ubhp-nova/claude-c/pjc3-docker/
AGENT_NAMESPACE=CLAUDE bash backup.sh
```

Erwartet bei laufendem Stack: Backup-Datei unter `/home/ubhp-nova/backups/docker/YYYY-MM-DD/vw-data.tar.gz`

Erwartet wenn Volume nicht existiert: `FEHLER: Volume 'CLAUDE-pjc3docker-vol-vw-data' nicht gefunden.` + Exit-Code 1

- [ ] **Step 4: Backup-Datei prüfen**

```bash
ls -lh /home/ubhp-nova/backups/docker/$(date +%Y-%m-%d)/
tar tzf /home/ubhp-nova/backups/docker/$(date +%Y-%m-%d)/vw-data.tar.gz | head -20
```

Erwartet: `vw-data.tar.gz` vorhanden, `tar tzf` zeigt Dateiliste ohne Fehler

- [ ] **Step 5: Commit**

```bash
cd /home/ubhp-nova/claude-c/pjc3-docker/
git add backup.sh
git commit -m "feat: add vaultwarden backup script"
```

---

## Chunk 2: Makefile-Target + Cron

### Task 3: Makefile-Target hinzufügen

**Files:**
- Modify: `/home/ubhp-nova/claude-c/pjc3-docker/Makefile`

- [ ] **Step 1: `.PHONY` Zeile um `backup` erweitern**

Aktuelle Zeile (Makefile:13):
```makefile
.PHONY: up up-monitor down ps logs guard
```

Ersetzen durch:
```makefile
.PHONY: up up-monitor down ps logs guard backup
```

- [ ] **Step 2: `backup` Target ans Ende des Makefiles anhängen**

Hinweis: Der bestehende `ifndef AGENT_NAMESPACE`-Guard am Makefile-Anfang (Zeile 4-6) gilt für alle Targets — kein innerer Guard nötig.

```makefile
backup:
	@echo "[$(AGENT_NS)/$(PROJ_ID)] Starte Backup..."
	@AGENT_NAMESPACE=$(AGENT_NS) bash backup.sh
	@echo "[$(AGENT_NS)/$(PROJ_ID)] Backup abgeschlossen."
```

- [ ] **Step 3: Target testen**

```bash
cd /home/ubhp-nova/claude-c/pjc3-docker/
AGENT_NAMESPACE=CLAUDE make backup
```

Erwartet: Gleiche Ausgabe wie direkter Skript-Aufruf

- [ ] **Step 4: Fehlerfall testen (AGENT_NAMESPACE fehlt)**

```bash
cd /home/ubhp-nova/claude-c/pjc3-docker/
make backup
```

Erwartet: `*** AGENT_NAMESPACE ist nicht gesetzt...` — kommt vom Top-Level-Guard (Makefile:4-6), gilt für alle Targets

- [ ] **Step 5: Commit**

```bash
git add Makefile
git commit -m "feat: add make backup target"
```

---

### Task 4: Cron-Eintrag einrichten

**Files:**
- Modify: crontab des aktuellen Users (kein Datei-Pfad, via `crontab -e`)

- [ ] **Step 1: Aktuellen Crontab anzeigen**

```bash
crontab -l 2>/dev/null || echo "Kein Crontab vorhanden"
```

- [ ] **Step 2: Cron-Eintrag hinzufügen**

```bash
(crontab -l 2>/dev/null; echo "0 2 * * * AGENT_NAMESPACE=CLAUDE /home/ubhp-nova/claude-c/pjc3-docker/backup.sh >> /home/ubhp-nova/backups/docker/backup.log 2>&1") | crontab -
```

- [ ] **Step 3: Eintrag prüfen**

```bash
crontab -l
```

Erwartet: Zeile `0 2 * * * AGENT_NAMESPACE=CLAUDE /home/ubhp-nova/claude-c/pjc3-docker/backup.sh >> /home/ubhp-nova/backups/docker/backup.log 2>&1` vorhanden

- [ ] **Step 4: Log-Datei anlegen (optional — Cron erstellt sie via `>>` automatisch)**

```bash
touch /home/ubhp-nova/backups/docker/backup.log
```

- [ ] **Step 5: Cron-Eintrag prüfen**

```bash
crontab -l
```

Erwartet: Zeile mit `0 2 * * * AGENT_NAMESPACE=CLAUDE ...backup.sh >> ...backup.log 2>&1` vorhanden

---

### Task 5: Docs committen

**Files:**
- Commit: `docs/superpowers/specs/2026-03-14-backup-design.md`
- Commit: `docs/superpowers/plans/2026-03-14-backup.md`

- [ ] **Step 1: Docs staged und committen**

```bash
cd /home/ubhp-nova/claude-c/pjc3-docker/
git add docs/superpowers/specs/2026-03-14-backup-design.md docs/superpowers/plans/2026-03-14-backup.md
git commit -m "docs: add backup spec and implementation plan"
```

---

## Manuelle Restore-Referenz

```bash
# Volume wiederherstellen
docker run --rm \
  -v CLAUDE-pjc3docker-vol-vw-data:/data \
  -v /home/ubhp-nova/backups/docker/YYYY-MM-DD:/backup:ro \
  alpine \
  tar xzf /backup/vw-data.tar.gz -C /data

# AGENT_NAMESPACE anpassen falls abweichend
```
